import Packet from './Packet';
export declare class DataPacket extends Packet {
    constructor();
    packets: any[];
    sequenceNumber: number;
    decode(): void;
    encode(): void;
    length(): number;
    static from(buffer: any): DataPacket;
}
