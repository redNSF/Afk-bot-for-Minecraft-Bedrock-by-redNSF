/// <reference types="node" />
import Packet from './Packet';
export default class ConnectionRequest extends Packet {
    constructor();
    clientGUID: bigint;
    requestTimestamp: bigint;
    decode(): void;
    encode(): void;
    static from(buffer: Buffer): ConnectionRequest;
}
