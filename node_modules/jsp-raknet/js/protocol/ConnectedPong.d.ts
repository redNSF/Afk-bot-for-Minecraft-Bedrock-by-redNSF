import Packet from "./Packet";
export default class ConnectedPong extends Packet {
    constructor();
    clientTimestamp: bigint;
    serverTimestamp: bigint;
    encode(): void;
    decode(): void;
    static from(buffer: any): ConnectedPong;
}
